name: testing

on:
  release:
    types: [created]

env:
  TEST_STR: base
jobs:
  dump:
    runs-on: ubuntu-latest
    steps:
      - name: version
        uses: actions/github-script@v4
        with:
          scripts: |
            const version = context.event.release.tag_name.substring(1)
            core.exportVariable('VERISION', version)
      - name: Dump github context
        run: echo "$GITHUB_CONTEXT"
        shell: bash
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
      - name: Dump env
        run: echo "$ENV"
        shell: bash
        env:
          GITHUB_CONTEXT: ${{ toJson(env) }}
  other:
    runs-on: ubuntu-latest
    steps:
      - name: Dump env
        run: echo "$ENV"
        shell: bash
        env:
          GITHUB_CONTEXT: ${{ toJson(env) }}
  other2:
    needs: dump
    runs-on: ubuntu-latest
    steps:
      - name: Dump env
        run: echo "$ENV"
        shell: bash
        env:
          GITHUB_CONTEXT: ${{ toJson(env) }}
